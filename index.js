// declaring then assigning - use camelCasing as needed

/** Rules 
 * Variables you create do not have quotes around time. Only sentence have quotations,
*/

/**Example 1 
 * Declare the variable cat
 * On a new line then assign cat to the number/integer/whole number 1,
*/
var cat; // declare
cat = 1; // assigning


/** Example 2 
 * Declare the variable light bulbs
 * On a new line assign light bulbs to 4
*/
var lightBulbs; // declare
lightbulbs = 4; // assign
console.log(lightBulbs)

/** Example 3 
 * Declare the variable brand
 * On a new line assign brand to Google
*/

var brand;
brand = "Google";
console.log(brand)

/** Example 4 
 * Declare the variable job title
 * On a new line then assign job title to website Developer
*/
var jobtitle;
jobTitle = 'Website Developer';
console.log(jobTitle)